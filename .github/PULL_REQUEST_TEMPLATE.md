## What and why

<!-- What changes, and the problem it solves. Link the issue: Fixes #123 -->

## How it was verified

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` and `pnpm build` pass
- [ ] `pnpm check:deploy` passes (if `infra/` or `deploy/` changed)
- [ ] Tests cover the new behaviour
- [ ] Screenshots attached (if the UI changed)
- [ ] README / README.ko updated (if users see the change)

<!-- If an AI assistant helped, say so here; you are still responsible for every line. -->
