# Handoff: 운영 CLI 도움말

- ID: 2026-09-17-1018-cli-help
- 상태: 부분 완료
- 기록 시각: 2026-09-17 10:18 Asia/Seoul
- 관련 Socratic: [2026-09-17-1018-cli-help](../socratic/2026-09-17-1018-cli-help.md)

## 목표와 결과

- 목표: 운영 CLI 세 개의 도움말을 DB 부작용 없이 제공하고 PR 리뷰·머지까지 완료.
- 결과: 구현·README·회귀 테스트 완료. 독립 worktree에서 작업하여 기존 세션의 체크아웃을 보존.

## 변경 사항

- `apps/api/src/cli/`: --help/-h를 설정 로드 전에 처리하고 정상 종료. 실제 CLI 프로세스 회귀 테스트 추가.
- `README.md`, `README.ko.md`: pnpm·Docker 도움말 사용법.

## 검증 증거

- `pnpm lint`, `pnpm typecheck` → 통과.
- `pnpm test` → API 149개, 웹 23개 통과. 최초 실행은 sandbox IPC 제한으로 실패하여 승인 후 재실행.
- 빌드된 CLI 3개 × --help/-h → 잘못된 PORT 설정에서도 6개 호출 모두 종료 코드 0.
- `pnpm db:backup --help`, `pnpm db:restore -h`, `pnpm auth:reset --help` → 통과.
- 자체 diff 리뷰 → 도움말 분기가 설정·DB 접근보다 앞에 위치하며 기존 실행 로직 유지 확인. 외부 승인 리뷰는 아님.

## 미검증 및 차단 요인

- 전체 빌드 재실행 중: sandbox 내 웹 빌드가 진행되지 않아 중단하고 승인 후 재실행.
- PR·원격 CI·머지 미완료. 열린 PR 조회 시 중복 PR 없음.

## 다음 세션 재개 순서

1. `/private/tmp/minidog-cli-help`에서 빌드 결과 확인.
2. 명시적 파일 스테이징·커밋 후 feat/cli-help를 push하고 main 대상 PR 생성.
3. PR diff 리뷰와 CI 확인 후 보호 규칙을 준수하여 머지. 기존 세션의 브랜치는 변경하지 않음.
