{
  "name": "@minidog/api",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --disable-warning=ExperimentalWarning src/index.ts",
    "start": "tsx --disable-warning=ExperimentalWarning src/index.ts",
    "build": "esbuild src/index.ts src/cli/reset-password.ts src/cli/backup.ts src/cli/restore.ts --bundle --platform=node --target=node24 --format=esm --outbase=src --outdir=dist --out-extension:.js=.mjs --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --log-level=warning",
    "typecheck": "tsc --noEmit",
    "test": "tsx --disable-warning=ExperimentalWarning --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@clickhouse/client": "^1.23.1",
    "@minidog/types": "workspace:*",
    "fastify": "^5.12.4",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@minidog/config": "workspace:*",
    "@types/node": "^24.0.0",
    "esbuild": "0.28.2",
    "tsx": "^4.23.13",
    "typescript": "~5.9.3"
  }
}
